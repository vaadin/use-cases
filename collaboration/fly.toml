# fly.toml app configuration file for collaboration-cases
#
# See https://fly.io/docs/reference/configuration/ for information about how to use this file.
#

app = 'collaboration-cases'
primary_region = 'iad'

[build]

[http_service]
  internal_port = 8080
  force_https = true
  auto_stop_machines = 'off'
  auto_start_machines = true
  min_machines_running = 1
  max_machines_running = 1
  processes = ['app']

[[vm]]
  memory = '1024'
  cpu_kind = 'shared'
  cpus = 2
  memory_mb = 1024
